spring:
  application:
    name: webapp
  datasource:
    url: jdbc:mysql://localhost:3306/cimiteroProva
    username: amministratoreCimitero
    password: Password_123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect